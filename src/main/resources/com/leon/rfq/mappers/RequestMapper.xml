<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.leon.rfq.mappers.RequestMapper">
	
	<resultMap type="RequestDetailImpl" id="RequestResultSet">
		<id 	property="identifier" column="requestId"/>
		<result property="request" column="request"/>
		<result property="status" column="status"/>
		<result property="pickedUpBy" column="pickedUpBy"/>
		<result property="clientId" column="clientId"/>
		<result property="bookCode" column="bookCode"/>
		<result property="tradeDate" column="tradeDate"/>
		<result property="premiumAmount" column="premiumAmount"/>
		<result property="delta" column="delta"/>
		<result property="gamma" column="gamma"/>
		<result property="vega" column="vega"/>
		<result property="theta" column="theta"/>
	</resultMap>
	
	<select id="get" parameterType="java.lang.String" resultType="RequestDetailImpl">
		SELECT * FROM requestforquotemain WHERE requestId=#{requestId}
	</select>
	
	<select id="requestExistsWithRequestId" parameterType="java.lang.String" resultType="RequestDetailImpl">
		SELECT * FROM requestforquotemain WHERE requestId=#{requestId}
	</select>
	
	<select id="getAll" resultMap="RequestResultSet">
		SELECT * FROM requestforquotemain
	</select>
	
<!-- 	<insert id="insert" parameterType="RequestDetailImpl">
		INSERT INTO requestforquotemain (requestId, request, firstName, lastName, groupName, locationName, isValid, lastUpdatedBy, lastUpdated)
		VALUES(#{userId}, #{emailAddress}, #{firstName}, #{lastName}, #{groupName}, #{locationName}, #{isValid}, #{lastUpdatedBy}, now() )
	</insert> -->
	
	<delete id="delete" parameterType="java.lang.String">
		DELETE FROM requestforquotemain
		WHERE requestId = #{requestId}
	</delete>		
			
</mapper>